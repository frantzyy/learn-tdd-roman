{
    "scripts": {
        "test": "node_modules/.bin/mocha --compilers js:babel-core/register src/**/*_test.js",
        "build": "babel src -d dist --presets es2015",
        "mutate": "stryker run"
    },
    "dependencies": {
        "babel-core": "^6.26.0",
        "babel-preset-es2015": "^6.24.1",
        "chai": "^4.1.2",
        "eslint": "^4.6.1",
        "mocha": "^3.5.3",
        "stryker": "^0.9.3",
        "stryker-api": "^0.8.0"
    },
    "devDependencies": {
        "stryker-api": "^0.8.0",
        "stryker-html-reporter": "^0.7.2",
        "stryker-mocha-framework": "^0.4.0",
        "stryker-mocha-runner": "^0.7.0"
    }
}